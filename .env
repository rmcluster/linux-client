# Tracker IP address and port
# Use docker gateway (host.docker.internal) for host access from container
TRACKER_IP=host.docker.internal:4917

# RPC server port
RPC_PORT=1984

# Storage (CAS) server port
CAS_PORT=1985
